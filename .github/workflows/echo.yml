# .github/workflows/basic.yml
name: Basic Workflow

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello DevOps 🚀"
